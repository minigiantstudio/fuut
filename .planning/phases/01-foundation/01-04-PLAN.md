---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: [apps/web/tests/auth.spec.ts, apps/api/src/index.ts]
autonomous: true
requirements: [AUTH-03, SETUP-01]
must_haves:
  truths:
    - "Full auth flow is verified by automated tests"
    - "Frontend can communicate with backend via secured routes"
  artifacts:
    - path: "apps/web/tests/auth.spec.ts"
      provides: "E2E authentication tests"
  key_links:
    - from: "Frontend"
      to: "Backend /api/me"
      via: "fetch with JWT"
---

<objective>
Finalize the foundation by verifying the end-to-end authentication flow and cross-domain connectivity between the frontend and backend. This ensures the environment is stable for subsequent phases.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E Auth Verification Tests</name>
  <files>apps/web/tests/auth.spec.ts</files>
  <action>
    Create Playwright tests in `apps/web/tests/auth.spec.ts` to verify:
    - Anonymous session is created on landing.
    - Nickname can be saved to profile.
    - Account promotion works (landing -> anonymous -> upgrade -> login).
    - Session persists across reloads.
    
    Use a test Supabase project or local mock if possible, otherwise use test accounts.
  </action>
  <verify>
    <automated>npx playwright test apps/web/tests/auth.spec.ts</automated>
  </verify>
  <done>Authentication flow verified by E2E tests</done>
</task>

<task type="auto">
  <name>Task 2: Cross-Domain Connectivity Check</name>
  <files>apps/web/src/hooks/useApi.ts, apps/api/src/index.ts</files>
  <action>
    Create a `useApi` hook in the frontend that wraps `fetch` and automatically includes the Supabase JWT.
    Implement a "Connectivity Check" in the UI that calls `GET /api/me`.
    Ensure the backend correctly identifies the user and returns their profile details.
  </action>
  <verify>
    <automated># Verify that /api/me returns 200 and the correct user object when called from FE</automated>
  </verify>
  <done>Frontend and Backend successfully communicating with authentication</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Runner -> System | Automated tests interacting with Auth and API. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial | API Endpoints | mitigate | Rate limiting on backend (to be added in later phase if needed). |
</threat_model>

<verification>
Run all tests in the monorepo: `npm test`.
Verify the app works in a production-like environment (Vite preview).
</verification>

<success_criteria>
All Playwright tests pass.
Connectivity check in UI shows "Backend Connected" for authenticated users.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
