---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [supabase/migrations/20260401000000_init_auth.sql, apps/api/src/index.ts, apps/api/src/middleware/auth.ts]
autonomous: true
requirements: [SETUP-03, SETUP-02]
must_haves:
  truths:
    - "Supabase profiles table exists with RLS enabled"
    - "Express backend validates Supabase JWTs"
  artifacts:
    - path: "supabase/migrations/20260401000000_init_auth.sql"
      provides: "Profiles table and RLS policies"
    - path: "apps/api/src/middleware/auth.ts"
      provides: "Supabase JWT validation middleware"
  key_links:
    - from: "apps/api/middleware/auth.ts"
      to: "Supabase Auth"
      via: "supabase.auth.getUser()"
---

<objective>
Setup the Supabase database schema and implement the Express backend with authentication middleware. This ensures that the backend can securely identify users from Supabase JWTs.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/research/01-foundation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Schema & RLS</name>
  <files>supabase/migrations/20260401000000_init_auth.sql</files>
  <action>
    Create a migration file to:
    - Create `public.profiles` table linked to `auth.users`.
    - Add fields: `id` (UUID), `nickname` (text), `avatar_url` (text), `locale` (text).
    - Enable Row Level Security (RLS) on `profiles`.
    - Create policies: 
      - Users can view any profile.
      - Users can only update their own profile.
    - Create a trigger to automatically create a profile when a new user signs up.
  </action>
  <verify>
    <automated># Manual verification in Supabase dashboard recommended if CLI not available. 
    # If CLI exists: supabase db lint</automated>
  </verify>
  <done>Database schema and RLS policies defined</done>
</task>

<task type="auto">
  <name>Task 2: Express Backend Scaffolding</name>
  <files>apps/api/src/index.ts, apps/api/package.json</files>
  <action>
    Initialize the Express backend in `apps/api`.
    Install dependencies: express, @supabase/supabase-js, cors, dotenv.
    Setup basic TypeScript config and an entry point that starts the server.
    Define a `/health` endpoint for connectivity checks.
  </action>
  <verify>
    <automated>curl -f http://localhost:3001/health</automated>
  </verify>
  <done>Express server running with TypeScript</done>
</task>

<task type="auto">
  <name>Task 3: Supabase Auth Middleware</name>
  <files>apps/api/src/middleware/auth.ts, apps/api/src/index.ts</files>
  <action>
    Implement `authMiddleware` in `apps/api/src/middleware/auth.ts`.
    The middleware should:
    - Extract the Bearer token from the `Authorization` header.
    - Call `supabase.auth.getUser(token)` to verify the JWT and fetch user details.
    - Attach the user object to the request.
    - Return 401 if the token is missing or invalid.
    
    Apply this middleware to a protected route `/api/me`.
  </action>
  <verify>
    <automated># Test with a dummy/invalid token
    curl -I -H "Authorization: Bearer invalid" http://localhost:3001/api/me | grep 401</automated>
  </verify>
  <done>Backend securely validates Supabase JWTs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input (JWT) crosses here; must be verified by Supabase. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Spoofing | Auth Middleware | mitigate | Use `supabase.auth.getUser()` instead of manual JWT decoding to ensure validity. |
| T-01-03 | Elevation | RLS Policies | mitigate | Strict RLS policies on `profiles` table to prevent unauthorized updates. |
</threat_model>

<verification>
Verify backend can start and connect to Supabase (via env vars).
Verify RLS policies are active by attempting unauthorized SQL (if possible via CLI).
</verification>

<success_criteria>
Backend `/api/me` returns 401 without token and 200 with a valid token.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
