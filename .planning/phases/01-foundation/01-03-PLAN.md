---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [apps/web/src/lib/supabase.ts, apps/web/src/hooks/useAuth.tsx, apps/web/src/components/Onboarding.tsx]
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-04]
must_haves:
  truths:
    - "User can join anonymously on first visit"
    - "User can upgrade to email account without losing progress"
    - "User can set a nickname in their profile"
  artifacts:
    - path: "apps/web/src/lib/supabase.ts"
      provides: "Supabase client instance"
    - path: "apps/web/src/hooks/useAuth.tsx"
      provides: "Authentication state and promotion logic"
  key_links:
    - from: "apps/web"
      to: "Supabase Auth"
      via: "supabase.auth.signInAnonymously()"
    - from: "apps/web"
      to: "Supabase Auth"
      via: "supabase.auth.updateUser()"
---

<objective>
Implement the frontend authentication flow, starting with frictionless anonymous entry and allowing users to "promote" their accounts to email/password. Also provide a simple UI for profile setup.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/research/01-foundation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Client & Auth Hook</name>
  <files>apps/web/src/lib/supabase.ts, apps/web/src/hooks/useAuth.tsx</files>
  <action>
    Initialize the Supabase client in `apps/web/src/lib/supabase.ts`.
    Create a `useAuth` hook that manages the user session.
    On first mount, if no session exists, trigger `supabase.auth.signInAnonymously()`.
    Expose user, loading state, and an `upgradeAccount(email, password)` function.
  </action>
  <verify>
    <automated># Check that supabase.auth.getSession() returns a session after mount</automated>
  </verify>
  <done>Anonymous session automatically created on mount</done>
</task>

<task type="auto">
  <name>Task 2: Account Promotion Flow</name>
  <files>apps/web/src/components/Onboarding.tsx, apps/web/src/hooks/useAuth.tsx</files>
  <action>
    Implement the `upgradeAccount` logic using `supabase.auth.updateUser({ email, password })`.
    Ensure this logic is called from a new `Onboarding` component or modal.
    Verify that the user's UUID remains the same after promotion (log to console during dev).
    Handle Supabase auth errors (e.g. "manual linking disabled") and display to user.
  </action>
  <verify>
    <automated># Manual verification: Sign in anonymously, then call upgradeAccount.
    # Check that user ID in auth.users matches before and after.</automated>
  </verify>
  <done>Anonymous users can upgrade to permanent email accounts</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Profile Setup UI</name>
  <files>apps/web/src/components/ProfileForm.tsx</files>
  <action>
    Create a `ProfileForm` component that allows users to set their `nickname`.
    This form should update the `public.profiles` table in Supabase.
    Ensure the nickname is displayed in the UI (e.g. in the TopBar).
  </action>
  <what-built>A profile form that updates the user's nickname in the database.</what-built>
  <how-to-verify>
    1. Start the app.
    2. Enter a nickname in the profile form.
    3. Save and refresh the page.
    4. Verify the nickname persists.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <done>User can customize their profile and data persists</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Supabase | User initiates auth state changes here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information | Auth Session | mitigate | Use secure, httpOnly cookies if possible (or Supabase default secure storage). |
| T-01-05 | Tampering | Profile Data | mitigate | RLS ensures users can only edit their own `profile_id`. |
</threat_model>

<verification>
Verify anonymous UUID is preserved during account promotion.
Verify profile updates are reflected in the UI and DB.
</verification>

<success_criteria>
Anonymous user can enter nickname, upgrade to email, and log back in with same nickname.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
