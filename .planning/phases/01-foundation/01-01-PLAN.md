---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, apps/api/package.json, apps/web/package.json, packages/types/package.json, packages/types/src/index.ts]
autonomous: true
requirements: [SETUP-01, SETUP-02]
must_haves:
  truths:
    - "Monorepo structure exists with shared types"
    - "Prototype is migrated to apps/web and builds"
  artifacts:
    - path: "package.json"
      provides: "npm workspaces configuration"
    - path: "packages/types/src/index.ts"
      provides: "Shared domain interfaces"
    - path: "apps/web/package.json"
      provides: "Frontend app definition"
  key_links:
    - from: "apps/web"
      to: "packages/types"
      via: "npm workspace dependency"
---

<objective>
Initialize the monorepo structure and migrate the prototype into the new workspace. Define shared types to ensure consistency between frontend and backend.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/intel/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Monorepo Structure</name>
  <files>package.json, apps/api/package.json, apps/web/package.json, packages/types/package.json</files>
  <action>
    Initialize npm workspaces in the root package.json. 
    Create the directory structure:
    - apps/api (Backend)
    - apps/web (Frontend)
    - packages/types (Shared)
    
    Setup basic package.json for each with appropriate naming (@fuut/api, @fuut/web, @fuut/types).
  </action>
  <verify>
    <automated>npm run -w @fuut/types --version && npm run -w @fuut/web --version</automated>
  </verify>
  <done>Workspaces initialized and reachable via npm -w</done>
</task>

<task type="auto">
  <name>Task 2: Define Shared Domain Types</name>
  <files>packages/types/src/index.ts</files>
  <action>
    Export core TypeScript interfaces from @fuut/types:
    - Profile (id, nickname, avatar_url, locale)
    - Match (id, home_team, away_team, kickoff_at, status)
    - Prediction (id, user_id, match_id, home_score, away_score, points)
    
    Ensure types match the planned Supabase schema.
  </action>
  <verify>
    <automated>tsc --project packages/types/tsconfig.json --noEmit</automated>
  </verify>
  <done>Core types exported and valid TypeScript</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Prototype to apps/web</name>
  <files>apps/web/*</files>
  <action>
    Move all files from prototype/ into apps/web/.
    Update apps/web/package.json to include dependencies from prototype/package.json.
    Ensure apps/web/src/ references @fuut/types for any shared interfaces.
    Fix any path aliases in vite.config.ts or tsconfig.json.
  </action>
  <verify>
    <automated>npm run -w @fuut/web build</automated>
  </verify>
  <done>Prototype migrated and successfully building in the workspace</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| packages/types | Internal trust; only shared definitions. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Shared Types | mitigate | Peer review and CI build checks. |
</threat_model>

<verification>
Verify `npm install` works from root and installs all workspace dependencies.
Verify `apps/web` can import from `@fuut/types`.
</verification>

<success_criteria>
`npm run build` from root completes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
