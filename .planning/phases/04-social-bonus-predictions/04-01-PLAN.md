---
phase: 04-social-bonus-predictions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260520000000_phase4_social_foundation.sql, supabase/seed.sql, packages/types/src/database.types.ts]
autonomous: true
requirements: [SOCIAL-01, LEAGUE-04]

must_haves:
  truths:
    - "Database has bonus_question_catalog table"
    - "Database has app_config table for global settings"
    - "Database has snapshot_tokens table"
    - "Leagues table has tier column"
    - "Matches have a way to relate to catalog (seeded bonus questions)"
  artifacts:
    - path: "supabase/migrations/20260520000000_phase4_social_foundation.sql"
      provides: "Schema for Social and Bonus features"
    - path: "supabase/seed.sql"
      provides: "Curated bonus question catalog data"
  key_links:
    - from: "matches.bonus_question"
      to: "bonus_question_catalog.id"
      via: "manual seeding/mapping"
---

<objective>
Establish the database foundation for Phase 4 social and bonus features.
Create the required tables and seed the curated bonus question catalog.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-bonus-predictions/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 4 foundation migration</name>
  <files>supabase/migrations/20260520000000_phase4_social_foundation.sql</files>
  <action>
    Create a new migration file with:
    1. Table `bonus_question_catalog` (id, prompt_text, category, created_at). per D-02.
    2. Table `app_config` (key TEXT PRIMARY KEY, value JSONB). Seed default `bonus_reveal_lead_minutes: 60` and `LEAGUE_FREE_MAX_MEMBERS: 10`. per D-07, D-21.
    3. Table `snapshot_tokens` (token TEXT PRIMARY KEY, league_id UUID, snapshot_payload JSONB, created_by UUID, created_at TIMESTAMPTZ DEFAULT NOW()). per D-14.
    4. Alter `leagues` table: add `tier` column (TEXT, NOT NULL, DEFAULT 'free', CHECK (tier IN ('free', 'premium'))). per D-21.
    5. Ensure RLS policies: `bonus_question_catalog` (public read), `app_config` (admin read/write, public read for lead time), `snapshot_tokens` (public read by token, authenticated write).
  </action>
  <verify>
    <automated>supabase db reset && supabase db test supabase/tests/phase4_schema_test.sql</automated>
  </verify>
  <done>Migration applied, tables exist with correct constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Seed curated bonus question catalog</name>
  <files>supabase/seed.sql</files>
  <action>
    Update `supabase/seed.sql` to populate `bonus_question_catalog` with ~30 curated yes/no questions (e.g., "Will there be a red card?", "Will both teams score?").
    Then, update the seed logic for `matches` to assign one `bonus_question` (prompt_text) to every match by selecting from the catalog.
    Note: Per D-03, we reuse the `matches.bonus_question` column.
  </action>
  <verify>
    <automated>supabase db reset && psql $DB_URL -c "SELECT count(*) FROM bonus_question_catalog;"</automated>
  </verify>
  <done>Catalog seeded and matches have assigned bonus questions.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate database types</name>
  <files>packages/types/src/database.types.ts</files>
  <action>
    Run the Supabase CLI command to regenerate TypeScript types based on the new schema.
  </action>
  <verify>
    <automated>npm run gen-types (or equivalent command in workspace)</automated>
  </verify>
  <done>TypeScript types reflect new tables and columns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Snapshot | Snapshot payload is JSONB, must be validated on write to prevent XSS in teaser pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `app_config` | mitigate | Ensure RLS only allows specific keys (like reveal lead time) to be read by non-admins if necessary. |
| T-04-02 | Tampering | `leagues.tier` | mitigate | Tier column must NOT be editable via public RLS; only via RPC or Admin subtree. |
</threat_model>

<verification>
Check that tables exist in Supabase studio and that `matches` in the DB have `bonus_question` populated.
</verification>

<success_criteria>
- Migration applied cleanly.
- Catalog contains 30+ questions.
- Every match in seed data has an assigned bonus question.
- Tier column exists on leagues with 'free' default.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-bonus-predictions/04-01-SUMMARY.md`
</output>
