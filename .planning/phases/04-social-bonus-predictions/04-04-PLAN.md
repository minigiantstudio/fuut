---
phase: 04-social-bonus-predictions
plan: 04
type: execute
wave: 3
depends_on: [04-01]
files_modified: [apps/web/src/components/tabs/PredictTab.tsx, apps/web/src/components/tabs/LeagueTab.tsx, apps/api/src/rpc/remove_member.sql, apps/api/src/rpc/rename_league.sql]
autonomous: true
requirements: [SOCIAL-02, LEAGUE-03, LEAGUE-04]

must_haves:
  truths:
    - "Peer predictions are revealed after a match is SCORED"
    - "League Admin can rename the league"
    - "League Admin can remove members"
    - "Admin sees 'Request premium' link when league is full"
  artifacts:
    - path: "apps/api/src/rpc/remove_member.sql"
      provides: "Soft member removal"
    - path: "apps/api/src/rpc/rename_league.sql"
      provides: "League renaming capability"
  key_links:
    - from: "LeagueTab"
      to: "rename_league RPC"
      via: "inline editing"
---

<objective>
Implement peer visibility for completed matches and enhance league administration tools.
Enable league admins to manage their group (rename, remove) and request upgrades.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-bonus-predictions/04-01-SUMMARY.md
@.planning/phases/04-social-bonus-predictions/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Peer visibility on SCORED matches</name>
  <files>apps/web/src/components/tabs/PredictTab.tsx</files>
  <action>
    Update SCORED match cards on `PredictTab` (per D-09, D-10):
    1. Make SCORED cards expandable on tap.
    2. Fetch and render a list of other members' predictions for that match.
    3. Sorting: Current User at top, then Admin, then alphabetically.
    4. Ensure peer predictions are ONLY fetched/shown if status is SCORED.
  </action>
  <verify>
    <automated>Check SCORED match on PredictTab. Expand and verify peer list appears.</automated>
  </verify>
  <done>Users can see each other's historical predictions to foster competition.</done>
</task>

<task type="auto">
  <name>Task 2: League Admin tools (Rename & Remove)</name>
  <files>apps/web/src/components/tabs/LeagueTab.tsx, apps/api/src/rpc/remove_member.sql, apps/api/src/rpc/rename_league.sql</files>
  <action>
    Implement management RPCs and UI (per D-18, D-19, D-20):
    1. RPC `rename_league(id, new_name)` (Admin only).
    2. RPC `remove_member(league_id, user_id)` (Admin only, soft-delete from `league_members`).
    3. In `LeagueTab.tsx`, show pencil icon next to league name for admins -> opens inline input.
    4. In `LeagueTab.tsx`, show trash icon next to members for admins -> opens confirmation dialog.
  </action>
  <verify>
    <automated>Rename league as admin, verify change. Remove member as admin, verify member disappears from list.</automated>
  </verify>
  <done>League admins have basic management capabilities.</done>
</task>

<task type="auto">
  <name>Task 3: 'Request Premium' mailto for admins</name>
  <files>apps/web/src/components/tabs/LeagueTab.tsx</files>
  <action>
    Handle upgrade requests (per D-22):
    1. If a league is `free` tier and at the member cap, show a "League Full - Request Premium" button in `LeagueTab`.
    2. Button is a `mailto:` link to `ADMIN_CONTACT_EMAIL` (env var).
    3. Prefill subject: `Premium upgrade — [CODE] ([NAME])`.
    4. Prefill body with league details.
  </action>
  <verify>
    <automated>Verify button appears when league full. Verify mailto link format.</automated>
  </verify>
  <done>Admins have a clear path to request league expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Admin RPCs | RPCs MUST verify that `auth.uid()` is an admin of the target league. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | `rename_league` | mitigate | Verify admin role inside SQL function using `league_members.role`. |
| T-04-06 | Tampering | `remove_member` | mitigate | Verify admin role inside SQL function. Prevent admin from removing self (use transfer logic if needed later). |
</threat_model>

<verification>
Check LeagueTab as an admin and non-admin. Verify icons only show for admin. Test rename and remove flows.
</verification>

<success_criteria>
- SCORED cards expand to show peers.
- Admin can rename league and remove members.
- Mailto link appears for full free leagues.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-bonus-predictions/04-04-SUMMARY.md`
</output>
