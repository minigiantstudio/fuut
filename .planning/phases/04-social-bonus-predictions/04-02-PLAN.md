---
phase: 04-social-bonus-predictions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/src/admin/AdminDashboard.tsx, apps/api/src/routes/admin.ts, apps/api/src/rpc/join_league_by_code.sql]
autonomous: true
requirements: [LEAGUE-04, SOCIAL-01]

must_haves:
  truths:
    - "Global Admin can update bonus reveal lead time"
    - "Global Admin can flip a league to premium tier"
    - "Joining a league enforces the member cap for free tier"
  artifacts:
    - path: "apps/api/src/rpc/join_league_by_code.sql"
      provides: "Cap enforcement on league join"
  key_links:
    - from: "Join UI"
      to: "join_league_by_code RPC"
      via: "error handling for 'league full'"
---

<objective>
Implement global administrative controls and server-side league tier enforcement.
This covers the reveal window configuration and the manual premium upgrade path.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-bonus-predictions/04-01-SUMMARY.md
@.planning/phases/04-social-bonus-predictions/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Join RPC with member cap enforcement</name>
  <files>apps/api/src/rpc/join_league_by_code.sql</files>
  <action>
    Modify `join_league_by_code` RPC (per D-21):
    1. Read `app_config` for `LEAGUE_FREE_MAX_MEMBERS` (default 10).
    2. Check target league `tier`.
    3. If `tier = 'free'`, count current members.
    4. If `count >= max`, raise exception with code 'LEAGUE_FULL'.
    5. Otherwise proceed with join.
  </action>
  <verify>
    <automated>Run join tests with a league that has 10 members and verify 'LEAGUE_FULL' error.</automated>
  </verify>
  <done>Join RPC enforces free-tier cap.</done>
</task>

<task type="auto">
  <name>Task 2: Global Admin UI for app config and tier flip</name>
  <files>apps/web/src/admin/AdminDashboard.tsx, apps/api/src/routes/admin.ts</files>
  <action>
    Extend the Global Admin subtree (per D-07, D-23):
    1. Add a setting input to edit `app_config.bonus_reveal_lead_minutes`.
    2. In the existing league management section (or a new section), add a "Flip to Premium" button for each league.
    3. Button calls a new admin-only API route `POST /admin/leagues/:id/tier` with payload `{ tier: 'premium' }`.
    4. Admin route verifies the isolated admin JWT (DEC-018).
  </action>
  <verify>
    <automated>Login to /admin, update reveal time, check DB. Click Flip to Premium, check DB league row tier.</automated>
  </verify>
  <done>Global Admin has control over reveal timing and league monetization state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → API | Admin actions must be authorized with the dedicated admin JWT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | `/admin` routes | mitigate | Ensure `requireAdminToken` middleware is applied to all new admin routes. |
</threat_model>

<verification>
Verify that a league with 10 members cannot be joined until flipped to premium in the admin dashboard.
</verification>

<success_criteria>
- Join RPC returns 'LEAGUE_FULL' when cap reached.
- Admin dashboard allows changing lead time.
- Admin dashboard can set league tier to 'premium'.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-bonus-predictions/04-02-SUMMARY.md`
</output>
