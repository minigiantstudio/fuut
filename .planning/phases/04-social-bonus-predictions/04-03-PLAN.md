---
phase: 04-social-bonus-predictions
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified: [apps/web/src/components/tabs/PredictTab.tsx, apps/web/src/components/tabs/RankingTab.tsx, apps/api/src/rpc/get_matches_with_bonus.sql]
autonomous: true
requirements: [SOCIAL-01, SOCIAL-04]

must_haves:
  truths:
    - "Bonus question text is hidden until the reveal lead time"
    - "PredictTab shows a locked placeholder for unrevealed bonuses"
    - "Users can answer revealed bonus questions inline on match cards"
    - "Sonner toasts appear when a match is scored"
  artifacts:
    - path: "apps/api/src/rpc/get_matches_with_bonus.sql"
      provides: "Redaction logic for bonus questions"
  key_links:
    - from: "PredictTab"
      to: "get_matches_with_bonus RPC"
      via: "data fetching"
    - from: "leaderboard_snapshots channel"
      to: "Sonner toast"
      via: "realtime subscription"
---

<objective>
Implement the micro-prediction user experience and live scoring feedback.
Includes server-side question redaction to prevent scraping and frontend reveal logic.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-bonus-predictions/04-01-SUMMARY.md
@.planning/phases/04-social-bonus-predictions/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side bonus question redaction</name>
  <files>apps/api/src/rpc/get_matches_with_bonus.sql</files>
  <action>
    Create or update the match-fetching RPC (per D-08):
    1. Calculate `reveal_at = kickoff_at - (SELECT value->>'bonus_reveal_lead_minutes' FROM app_config)::int * interval '1 minute'`.
    2. In the SELECT, return `bonus_question` ONLY IF `now() >= reveal_at`.
    3. Return a flag `is_bonus_revealed` (boolean).
    4. Return `reveal_at` timestamp for the client countdown.
  </action>
  <verify>
    <automated>Call RPC for a match kicking off in 2 hours with lead time 60 min. Verify `bonus_question` is NULL.</automated>
  </verify>
  <done>Bonus question text is redacted server-side until the reveal window opens.</done>
</task>

<task type="auto">
  <name>Task 2: PredictTab bonus question UI</name>
  <files>apps/web/src/components/tabs/PredictTab.tsx</files>
  <action>
    Update the match card on `PredictTab` (per D-06, D-08):
    1. If `is_bonus_revealed` is false, render locked placeholder: `🔒 Surprise bonus reveals in Xh Ym` (retro style).
    2. If `is_bonus_revealed` is true, render the bonus question text + Yes/No toggle.
    3. Use existing persistence logic (Phase 3 wired `predictions.bonus_answer`).
  </action>
  <verify>
    <automated>Check PredictTab in browser. Verify countdown for far matches, question for near matches.</automated>
  </verify>
  <done>Users see countdown or answerable bonus question inline on match cards.</done>
</task>

<task type="auto">
  <name>Task 3: Scoring Feedback (Sonner toasts)</name>
  <files>apps/web/src/components/tabs/RankingTab.tsx</files>
  <action>
    In `RankingTab.tsx` (per D-15, D-16):
    1. In the existing `leaderboard_snapshots` channel subscription:
    2. When a new payload arrives (on match scored), extract `pts_earned` and `rank_delta` for the current user.
    3. Call `toast.success` with: `Match X scored — you earned Y pts (#N ↑/↓)`.
    4. Ensure this only triggers for the relevant user.
  </action>
  <verify>
    <automated>Mock a realtime payload in browser console and verify Sonner toast appearance.</automated>
  </verify>
  <done>Users receive real-time feedback when matches are scored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Client | Bonus question text MUST NOT be sent to client before reveal time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | Match RPC | mitigate | Redaction MUST happen in DB/API, not just hidden in CSS on frontend. |
</threat_model>

<verification>
Check PredictTab for upcoming matches and verify the countdown. Use `supabase db reset` with modified seed dates to test reveal transition.
</verification>

<success_criteria>
- Bonus questions are hidden until lead time.
- Match cards show countdown.
- Sonner toast appears on scoring events.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-bonus-predictions/04-03-SUMMARY.md`
</output>
