---
phase: 04-social-bonus-predictions
plan: 05
type: execute
wave: 3
depends_on: [04-01]
files_modified: [apps/web/src/components/tabs/RankingTab.tsx, apps/web/src/routes/Snapshot.tsx, apps/api/src/routes/snapshots.ts]
autonomous: true
requirements: [SOCIAL-03]

must_haves:
  truths:
    - "Users can generate a pixel-art ranking snapshot"
    - "Snapshot is shareable via Web Share API or fallback links"
    - "Public snapshot URLs (/s/:token) show a teaser landing page"
    - "Snapshots are tokenized and frozen in the database"
  artifacts:
    - path: "apps/web/src/routes/Snapshot.tsx"
      provides: "Teaser landing page for shared rankings"
    - path: "apps/api/src/routes/snapshots.ts"
      provides: "Snapshot token generation and retrieval"
  key_links:
    - from: "RankingTab"
      to: "/api/snapshots"
      via: "POST to generate token"
    - from: "Social Media"
      to: "/s/:token"
      via: "public route"
---

<objective>
Implement shareable ranking snapshots. This includes client-side image generation,
server-side tokenization for frozen history, and a public teaser route for social sharing.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-bonus-predictions/04-01-SUMMARY.md
@.planning/phases/04-social-bonus-predictions/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot generation logic</name>
  <files>apps/web/src/components/tabs/RankingTab.tsx</files>
  <action>
    Implement ranking snapshot capture (per D-11, D-12, D-13):
    1. Use `html-to-image` (or similar) to render a pixel-art style PNG of the ranking.
    2. Layout: Top-3 podium + "You" row + last matchday recap.
    3. Trigger via a new "Share" button on `RankingTab`.
    4. Integration with Web Share API (`navigator.share`) for mobile.
    5. Fallback buttons (WhatsApp, Telegram, Copy Link) for desktop.
  </action>
  <verify>
    <automated>Click Share button on mobile emulator, check for share sheet. On desktop, check for fallback buttons.</automated>
  </verify>
  <done>Users can generate and initiate sharing of ranking snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot tokenization API</name>
  <files>apps/api/src/routes/snapshots.ts</files>
  <action>
    Create snapshot persistence API (per D-11, D-14):
    1. `POST /api/snapshots` -> accepts leaderboard payload, generates random short token, stores in `snapshot_tokens`.
    2. Payload is a "frozen copy" to ensure the URL remains valid/accurate to the share time.
    3. `GET /api/snapshots/:token` -> returns the frozen payload.
  </action>
  <verify>
    <automated>POST to /api/snapshots, verify token creation in DB. GET with token, verify payload match.</automated>
  </verify>
  <done>Snapshots are persisted as frozen tokens for public sharing.</done>
</task>

<task type="auto">
  <name>Task 3: Public teaser route (/s/:token)</name>
  <files>apps/web/src/routes/Snapshot.tsx</files>
  <action>
    Create the public landing page (per D-11):
    1. New route `/s/:token`.
    2. Fetches frozen payload from API.
    3. Renders a teaser view of the ranking (pixel art style).
    4. Includes OG meta tags (title, description, image) for social previews.
    5. CTA: "Join this league ->" linking to the app's join flow.
  </action>
  <verify>
    <automated>Visit /s/[token] in browser, verify teaser rendering and OG tags in source.</automated>
  </verify>
  <done>Shared links lead to a teaser page that drives new user acquisition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public → API | Public teaser route fetches specific tokenized data. No sensitive user info (emails) should be in payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | Snapshot payload | mitigate | Ensure only public nicknames and scores are stored in `snapshot_payload`. |
| T-04-08 | XSS | Teaser page | mitigate | Sanitize nickname strings before rendering on the public teaser page. |
</threat_model>

<verification>
Generate a share link, open in incognito window, and verify the teaser page and social preview tags.
</verification>

<success_criteria>
- "Share" button produces a PNG.
- Short tokenized URL is generated.
- Public route /s/:token works without auth.
- OG tags render correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-bonus-predictions/04-05-SUMMARY.md`
</output>
