---
phase: 03-scoring-real-time-rankings
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: [apps/web/src/components/tabs/PredictTab.tsx, apps/web/src/components/tabs/RankingTab.tsx, apps/web/src/components/BonusPrediction.tsx]
autonomous: true
requirements: [SCORE-03]

must_haves:
  truths:
    - "Bonus predictions are persisted to the database and locked at kickoff"
    - "Leaderboard updates instantly when point changes occur via Supabase Realtime"
  artifacts:
    - path: "apps/web/src/components/BonusPrediction.tsx"
      provides: "Functional bonus prediction UI with DB persistence"
    - path: "apps/web/src/components/tabs/RankingTab.tsx"
      provides: "Real-time reactive leaderboard"
---

<objective>
Connect the frontend to the real-time backend. Make bonus predictions functional and 
ensure the leaderboard reacts instantly to point updates using Supabase Realtime.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-real-time-rankings/03-CONTEXT.md
@.planning/phases/03-scoring-real-time-rankings/03-RESEARCH.md
@apps/web/src/components/tabs/PredictTab.tsx
@apps/web/src/components/tabs/RankingTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Functional Bonus Predictions</name>
  <files>apps/web/src/components/BonusPrediction.tsx, apps/web/src/components/tabs/PredictTab.tsx</files>
  <action>
    Integrate the `BonusPrediction` component with Supabase:
    1. Update `BonusPrediction` to accept `initialAnswer`, `onSave`, and `disabled`.
    2. In `PredictTab`, pass the `bonus_answer` from the `prediction` record.
    3. Implement `onSave` in `PredictTab` to update the `bonus_answer` column in the `predictions` table (D-09).
    4. Ensure bonus predictions lock at the same time as match scores (kickoff).
    5. Update `BonusPrediction` to use the `bonus_question` field from the `matches` table (fallback to the hardcoded list if null).
  </action>
  <verify>
    <automated>ls apps/web/src/components/BonusPrediction.tsx</automated>
  </verify>
  <done>Bonus predictions are saved to the database and correctly displayed.</done>
</task>

<task type="auto">
  <name>Task 2: Real-time Leaderboard Updates</name>
  <files>apps/web/src/components/tabs/RankingTab.tsx</files>
  <action>
    Implement Supabase Realtime (CDC) for the leaderboard (D-06):
    1. In `RankingTab.tsx`, set up a `supabase.channel` to listen for `INSERT` or `UPDATE` events on the `leaderboard_snapshots` table for the current `league_id`.
    2. When a change is detected, invalidate the `['leaderboard', session.leagueId]` query via React Query's `queryClient`.
    3. Verify the `rank_delta` movement indicators are working as intended based on the updated snapshot data.
  </action>
  <verify>
    <automated>grep "supabase.channel" apps/web/src/components/tabs/RankingTab.tsx</automated>
  </verify>
  <done>Leaderboard reacts to backend point updates without manual refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → Client | Realtime events should be filtered by RLS or channel parameters to only expose relevant league data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Realtime Channel | mitigate | Use RLS on `leaderboard_snapshots` table to ensure users only receive updates for leagues they belong to. |
</threat_model>

<verification>
1. Submit a bonus prediction; refresh; verify it persists.
2. Open the app in two windows; update a score in one (via admin); verify the leaderboard updates in the other instantly.
</verification>

<success_criteria>
- Bonus predictions are persisted and locked correctly.
- Real-time leaderboard updates are visible within 2 seconds of a backend change.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-real-time-rankings/03-04-SUMMARY.md`
</output>
