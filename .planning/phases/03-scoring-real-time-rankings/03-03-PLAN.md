---
phase: 03-scoring-real-time-rankings
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [apps/web/src/App.tsx, apps/web/src/pages/Admin.tsx, apps/api/src/routes/admin.ts, apps/api/src/index.ts]
autonomous: true
requirements: [SCORE-04]

must_haves:
  truths:
    - "Global Admins can manually enter match scores and bonus results"
    - "Manual overrides trigger re-scoring and leaderboard updates"
    - "Non-admin users are strictly blocked from the /admin route"
  artifacts:
    - path: "apps/web/src/pages/Admin.tsx"
      provides: "Global Admin Dashboard UI"
    - path: "apps/api/src/routes/admin.ts"
      provides: "Secure admin scoring endpoints"
  key_links:
    - from: "apps/web/src/pages/Admin.tsx"
      to: "apps/api/src/routes/admin.ts"
      via: "fetch /api/admin"
      pattern: "fetch.*/api/admin"
---

<objective>
Create a fallback mechanism for match results. The Global Admin Dashboard allows manual entry
of scores and bonus results, ensuring the game continues even if the external API fails.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-real-time-rankings/03-CONTEXT.md
@.planning/phases/03-scoring-real-time-rankings/03-RESEARCH.md
@apps/api/src/services/scoring.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Admin Scoring Backend Routes</name>
  <files>apps/api/src/routes/admin.ts, apps/api/src/index.ts</files>
  <action>
    Create a secure admin router (D-07, D-08):
    1. Middleware: Check `is_global_admin` flag on the user record in Supabase.
    2. Endpoint: `POST /api/admin/match-result`
       - Input: `matchId`, `homeScore`, `awayScore`, `bonusResult` (boolean).
       - Updates `matches` table: sets scores, `bonus_result`, `is_final: true`, and `is_manual_override: true`.
       - Calls `ScoringService.scoreMatch(matchId)` immediately.
       - Calls `ScoringService.recalculateLeaderboard()` for all leagues.
    3. Mount the router in `apps/api/src/index.ts`.
  </action>
  <verify>
    <automated>grep "adminRouter" apps/api/src/index.ts</automated>
  </verify>
  <done>Admin API endpoints secured and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Create Admin Dashboard UI</name>
  <files>apps/web/src/pages/Admin.tsx, apps/web/src/App.tsx</files>
  <action>
    Create the `/admin` page for Global Admins:
    1. UI: List all matches (especially those not yet final).
    2. Controls: Inputs for Home Score, Away Score, and a Yes/No toggle for Bonus Result.
    3. "Finalize Result" button: Calls the admin API endpoint.
    4. Guard: Use `SessionContext` to check if `is_global_admin` is true. Redirect non-admins to `/`.
    5. Register route in `apps/web/src/App.tsx`.
    6. Maintain retro 8-bit aesthetic (D-03).
  </action>
  <verify>
    <automated>ls apps/web/src/pages/Admin.tsx</automated>
  </verify>
  <done>Admin UI implemented with proper security guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Admin API | Sensitive scoring actions must be authenticated and authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege | Admin API | mitigate | Backend MUST verify `is_global_admin` flag from DB, not just trust client header. |
</threat_model>

<verification>
1. Log in as a non-admin user; attempt to visit `/admin` (should redirect/error).
2. Log in as a global admin; manually score a match; verify points are recalculated in the leaderboard.
</verification>

<success_criteria>
- Only global admins can access `/admin`.
- Manual score entry successfully triggers re-scoring and leaderboard recalculation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-real-time-rankings/03-03-SUMMARY.md`
</output>
