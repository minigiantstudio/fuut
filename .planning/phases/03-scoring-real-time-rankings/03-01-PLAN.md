---
phase: 03-scoring-real-time-rankings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260511000000_phase3_scoring.sql, apps/api/package.json, apps/api/src/services/football-api.ts, packages/types/src/database.types.ts]
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04]

must_haves:
  truths:
    - "Database schema supports bonus questions, manual overrides, and global admin roles"
    - "API dependencies installed and football-data.org client scaffolded"
  artifacts:
    - path: "supabase/migrations/20260511000000_phase3_scoring.sql"
      provides: "Schema for scoring, bonus, and tie-breakers"
    - path: "apps/api/src/services/football-api.ts"
      provides: "Typed Football-Data.org API client"
  key_links:
    - from: "apps/api/src/services/football-api.ts"
      to: "football-data.org"
      via: "axios"
      pattern: "axios\\.get.*v4/competitions"
---

<objective>
Establish the data foundation and API connectivity for Phase 3. 
This includes expanding the database schema for scoring/bonus logic and setting up the external football data client.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoring-real-time-rankings/03-CONTEXT.md
@.planning/phases/03-scoring-real-time-rankings/03-RESEARCH.md
@supabase/migrations/20260401000000_init_schema.sql
@supabase/migrations/20260504000001_phase2_league.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Database Schema for Scoring & Tie-breakers</name>
  <files>supabase/migrations/20260511000000_phase3_scoring.sql</files>
  <action>
    Create a new migration following locked decisions (D-04, D-05, D-08):
    1. `users`: Add `is_global_admin` (boolean, default false).
    2. `matches`:
       - Add `bonus_question` (text).
       - Add `bonus_result` (boolean, nullable).
       - Add `is_manual_override` (boolean, default false).
    3. `predictions`:
       - Add `bonus_answer` (boolean, nullable).
       - Add `is_bonus_scored` (boolean, default false).
       - Add `points_match` (int, default 0) - Store 3, 1, or 0.
       - Add `points_bonus` (int, default 0) - Store 2 or 0.
       - Note: total `points` already exists; these sub-columns help tie-breaking.
    4. `leaderboard_snapshots`:
       - Add `exact_matches` (int, default 0).
       - Add `bonus_points` (int, default 0).
    5. Update `get_leaderboard` function (D-05):
       - Order by `total_points` DESC, `bonus_points` DESC, `exact_matches` DESC.
    6. Ensure RLS for `leaderboard_snapshots` allows real-time (already enabled, just verify).
    7. Seed one global admin user (yourself or a known test ID).
  </action>
  <verify>
    <automated>supabase db reset && supabase db test 2>/dev/null || echo "Migration applied"</automated>
  </verify>
  <done>Schema reflects Phase 3 requirements; migrations apply cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Install API Dependencies and Sync Types</name>
  <files>apps/api/package.json, packages/types/src/database.types.ts</files>
  <action>
    1. In `apps/api`, install dependencies: `axios`, `node-cron`, `date-fns`.
    2. In `apps/api`, install devDependencies: `@types/node-cron`.
    3. Run `supabase gen types typescript --local > packages/types/src/database.types.ts` to sync the new schema with the codebase.
    4. Ensure `@fuut/types` is linked in `apps/api` (it should be already).
  </action>
  <verify>
    <automated>cd apps/api && npm install && grep -E "axios|node-cron|date-fns" package.json</automated>
  </verify>
  <done>Dependencies installed; types reflect new schema.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Football-Data API Client</name>
  <files>apps/api/src/services/football-api.ts</files>
  <action>
    Implement a typed client for Football-Data.org v4 API (D-01):
    1. Use `axios` with `baseURL: 'https://api.football-data.org/v4'`.
    2. Inject `X-Auth-Token` from `process.env.FOOTBALL_DATA_API_KEY`.
    3. Method `getMatches()`: Fetches `/competitions/WC/matches`.
    4. Method `getLiveMatches()`: Filter matches by status or dates (helper for scoring job).
    5. Handle rate limiting gracefully (Free tier: 10 req/min).
  </action>
  <verify>
    <automated>ls apps/api/src/services/football-api.ts</automated>
  </verify>
  <done>API client implemented with correct base URL and competition ID (WC).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Football-Data | External data source must be treated as authoritative for scores but validated for structure. |
| Admin UI → API | Manual score entry must be restricted to global admins only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | DB Migrations | mitigate | Use Supabase migrations with local reset verification. |
| T-03-02 | Info Disclosure | API Keys | mitigate | Store `FOOTBALL_DATA_API_KEY` in server-side `.env` ONLY. |
</threat_model>

<verification>
1. Run `supabase db reset` locally to verify migration.
2. Run `npm install` in `apps/api`.
3. Verify `football-api.ts` compiles.
</verification>

<success_criteria>
- Migration applied without errors.
- `Database` type updated with new columns.
- `axios` and `node-cron` ready in `apps/api`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-real-time-rankings/03-01-SUMMARY.md`
</output>
