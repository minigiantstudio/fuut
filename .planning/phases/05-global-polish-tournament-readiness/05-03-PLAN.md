---
phase: 05-global-polish-tournament-readiness
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified: [apps/web/src/lib/i18n/fr.json, apps/web/src/lib/i18n/index.tsx, apps/web/src/components/LanguageToggle.tsx, apps/web/src/components/tabs/PredictTab.tsx, apps/web/src/components/tabs/RankingTab.tsx, apps/web/src/components/tabs/LeagueTab.tsx]
autonomous: true
requirements: [POLISH-02]

must_haves:
  truths:
    - "App ships in three locales: EN, ES, FR"
    - "Every user-facing string goes through t() — no hardcoded English left"
    - "EnsureComplete compile-time check covers FR with the same key set as EN/ES"
    - "LanguageToggle offers FR as a third option"
  artifacts:
    - path: "apps/web/src/lib/i18n/fr.json"
      provides: "French locale, key-parity with en/es"
    - path: "apps/web/src/lib/i18n/index.tsx"
      provides: "Lang union widened to en|es|fr"
  key_links:
    - from: "LanguageToggle"
      to: "i18n.setLang('fr')"
      via: "user selection"
---

<objective>
Add French as the third locale and sweep the remaining hardcoded English
strings out of the components. With EN/ES already in main, adding FR is mostly
a JSON file + a 1-line union widening — but the string sweep is the part that
makes the locale switch *actually* localize the whole UI.
</objective>

<execution_context>
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/jaiversin/Development/fuut/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-global-polish-tournament-readiness/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add French locale file</name>
  <files>apps/web/src/lib/i18n/fr.json</files>
  <action>
    1. Copy `apps/web/src/lib/i18n/en.json` to `fr.json`.
    2. Translate every value to French. Preferred register: formal/neutral
       ("Bonus surprise" not slang). Keep emoji prefixes (`🏆`, `⚽`, `👥`,
       `🔒`) verbatim — they're already universal.
    3. Specific tricky strings:
       - `bonus.reveals_in` — "Le bonus surprise se révèle dans" (the time
         suffix is appended via concatenation in BonusPrediction, so this must
         read naturally when followed by "Xh Ym").
       - `predict.peer_title` — "Pronostics de la ligue".
       - `league.request_premium` — "Ligue pleine — Demander premium".
       - `snapshot.join_cta` — "Rejoindre cette ligue →".
    4. Do NOT add keys not present in en.json — `EnsureComplete` will fail compile.
       If a key is missing in fr.json, compile-time error names it.
  </action>
  <verify>
    <automated>`bunx tsc --noEmit` in apps/web — EnsureComplete will fail if any key is missing in fr.json.</automated>
  </verify>
  <done>Third locale exists with key parity to EN/ES.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FR into i18n module + LanguageToggle</name>
  <files>apps/web/src/lib/i18n/index.tsx, apps/web/src/components/LanguageToggle.tsx</files>
  <action>
    1. `apps/web/src/lib/i18n/index.tsx`:
       - `import fr from './fr.json';`
       - Widen `Lang`: `export type Lang = 'en' | 'es' | 'fr';`
       - Add `fr` to the `strings` record.
       - Update `detectLang()` to also accept `'fr'` from `localStorage` and
         detect via `navigator.language.startsWith('fr')`. Order: cached → fr →
         es → en (fallback).
    2. `apps/web/src/components/LanguageToggle.tsx`:
       - Add FR as the third option in whatever UI shape it uses (segmented
         control / dropdown).
       - Keep the existing EN/ES toggle behavior; add a third button/option.
       - Persist the selection via the existing `setLang` API.
    3. No new top-level UI — the toggle keeps its current placement
       (TopBar / wherever it lives).
  </action>
  <verify>
    <automated>Open the app, toggle EN → FR → ES → EN. All visible text updates per toggle. localStorage key `fuut:lang` reflects the choice.</automated>
  </verify>
  <done>Users can switch to French and the whole UI follows.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep hardcoded English strings</name>
  <files>apps/web/src/components/tabs/PredictTab.tsx, apps/web/src/components/tabs/RankingTab.tsx, apps/web/src/components/tabs/LeagueTab.tsx</files>
  <action>
    Audit and translate the remaining hardcoded English strings the survey
    flagged. For each, add a new key to en/es/fr and replace the literal:
    1. **PredictTab.getStageName** — knockout stage labels currently inlined:
       - `"Round of 16"` → `predict.stage_r16`
       - `"Quarter-finals"` → `predict.stage_qf`
       - `"Semi-finals"` → `predict.stage_sf`
       - `"Final"` → `predict.stage_final` (or reuse `predict.final_label`)
    2. **PredictTab** — `"Showing all {group} matches"` subtitle for the group
       filter. Construct via t() + concatenation since interpolation isn't
       supported by the current `t()` (per the LeagueTab pattern).
    3. **RankingTab.handleShare** — error toasts:
       - `"Could not generate snapshot"` → `ranking.share_error`
       - Title/text passed to `navigator.share` (`${session.leagueName} — Fuut 2026`)
         is brand-y; OK to keep English for the share string itself (it goes
         out into WhatsApp/Telegram and matches the OG title).
    4. **LeagueTab** — regenerate-confirm dialog body and "Manage" header
       (already partly localized). Translate the remaining "Regenerate invite
       code?" title and the body copy.
    5. Skip strings in `apps/web/src/components/ui/*` (shadcn primitives) —
       they're library-level, low user-facing impact, and translating them
       fights upstream updates. Document as a follow-up if needed.
    6. Skip dev-only `console.warn` / `console.error` strings — never user-facing.
  </action>
  <verify>
    <automated>Grep apps/web/src/components for capitalized hardcoded strings: `grep -rEo '"[A-Z][a-z]{3,}[^"]{3,}"' apps/web/src/components/ | grep -v "from\|import\|className\|console\."` — should return only the deliberately-skipped library / dev strings.</automated>
  </verify>
  <done>The locale toggle truly localizes the whole UI; no English bleeds through when FR or ES is active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed — pure client-side i18n changes. The locale value lives in `localStorage` (per-device, not per-account).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | Locale leakage | n/a | Locale choice is non-sensitive. localStorage scope is per-origin. |
</threat_model>

<verification>
1. `bunx tsc --noEmit` — EnsureComplete validates FR ↔ EN key parity.
2. Toggle EN → FR → ES → EN in the running app; every visible string
   re-renders. Verify Predict tab knockout stages, Ranking share button, League
   regenerate dialog, Snapshot teaser page.
3. Reload the page after toggling FR — selection persists.
4. `navigator.language` auto-detection: in a fresh browser profile with locale
   `fr-FR`, the app boots in FR.
</verification>

<success_criteria>
- `fr.json` exists with the same key set as `en.json` (compile-time enforced).
- `LanguageToggle` offers EN / ES / FR with persistence.
- A `grep` for capitalized hardcoded strings in `apps/web/src/components/`
  returns only deliberately-skipped (library / dev) hits.
- The Snapshot teaser page localizes when its loader picks up the language.
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-polish-tournament-readiness/05-03-SUMMARY.md`
</output>
